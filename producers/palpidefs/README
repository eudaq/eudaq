You need to add the driver location as a variable to cmake with
cmake -DBUILD_palpidefs=on -DCMAKE_PALPIDEFS_DRIVER_INCLUDE:FILEPATH=<<DRIVER LOCATION>> .

You need to install tinyxml and unbuffer (Ubuntu package: libtinyxml-dev expect-dev)


Compile pALPIDEfs-software with 'make lib'