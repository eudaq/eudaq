get_filename_component(EUDAQ_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
set(EUDAQ_INCLUDE_DIR "@CONF_INCLUDE_DIRS@")
set(EUDAQ_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")
set(EUDAQ_LIBRARIES "@CONF_LIBRARIES@")
set(EUDAQ_LIBRARY_DIR "@CONF_LIBRARY_DIRS@")
set(EUDAQ_LIBRARY_DIRS "@CONF_LIBRARY_DIRS@")

# Our library dependencies (contains definitions for IMPORTED targets)
if(EXISTS ${CMAKE_CURRENT_LIST_DIR}/eudaqTargets.cmake)
  include("${CMAKE_CURRENT_LIST_DIR}/eudaqTargets.cmake")
else()
  include_directories(SYSTEM ${EUDAQ_INCLUDE_DIRS})
  add_library(eudaq::core ALIAS core)
endif()
